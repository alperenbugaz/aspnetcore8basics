@page
@model DetailsModel

<h1>Details Page</h1>

<div class="row">

    <div class="col-md-4">
        <div class="card">
            <img src="~/img/@Model.Employee.Photo" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">@Model.Employee.EmployeeName</h5>
                <p class="card-text">@Model.Employee.Email</p>
                <p class="card-text">@Model.Employee.Department</p>
                <p class="card-text">@Model.Employee.Email</p>

                <a href="/employees/"  class="btn btn-primary">Employees</a>
            </div>

        </div>
    </div>
    
</div>